# include <stdio.h>

int main(void)
{
	int i = 10;
	int k = 20;
	int m;

//	m = (3>2) && (k=0);   //C语言中非零是真，零是假，所以m是假

//	m = (3>2) && (k=8);  //C语言中真是用1表示，假是用0表示，所以m只可能是1或者0，其中(3>2)为真，k=8为真，&&之后为真

	m = (1>2) && (k=9);  //&&中，如果左边是假，右边就不会执行，所以这里输出的k=20

	printf("m = %d, k = %d\n",m,k); //输出的k值是后面赋给的值，并不是前面定义的值

	return 0;
}